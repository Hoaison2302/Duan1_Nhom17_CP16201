<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duan1_nhom17_cp16201">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.HuongDanActivity" android:theme="@style/FullScren"></activity>
        <activity android:name=".activity.PlayActivity" android:theme="@style/FullScren"/>
        <activity android:name=".activity.DiemActivity" android:theme="@style/FullScren"/>
        <activity
            android:name=".activity.FlashActivity"
            android:theme="@style/FullScren">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/FullScren" />
    </application>

</manifest>